#include <iostream>
#include <vector>
#include <limits.h>
#include <algorithm>
#include <string>
#include <math.h>
#include <limits.h>
#include <queue>
#include <map>
#include <set>
#include <iomanip>
#include <bitset>
#include <cassert>
#include <random>
#include <functional>
#include <stack>
#include <iomanip>
#include <cassert>
using namespace std;
long long MOD = 1e9 + 7;

set<int> A;

int main() {
	int s;
	cin >> s;
	int c = 1;
	A.insert(s);
	while (true) {
		int next;
		if (s % 2 == 0)next = s / 2;
		else next = 3 * s + 1;
		c++;
		if (A.find(next) != A.end()) {
			cout << c << endl;
			return 0;
		}
		A.insert(next);
		s = next;
	}
}
