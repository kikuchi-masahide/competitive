#include <iostream>
#include <vector>
#include <limits.h>
#include <algorithm>
#include <string>
#include <math.h>
#include <limits.h>
#include <queue>
#include <map>
#include <set>
#include <iomanip>
#include <bitset>
#include <cassert>
#include <random>
#include <functional>
#include <stack>
#include <iomanip>
#include <cassert>
using namespace std;
long long MOD = 1e9 + 7;

long long N,K;

int main() {
	cin >> N >> K;
	long long div = N * N * N;
	long long div2 = 1 + 3 * (N - 1) + 6 * (N - K) * (K - 1);
	cout << setprecision(12) << (long double)div2 / div << endl;
	return 0;
}
