#include <iostream>
#include <vector>
#include <limits.h>
#include <algorithm>
#include <string>
#include <math.h>
#include <limits.h>
#include <queue>
#include <map>
#include <set>
#include <iomanip>
#include <bitset>
#include <cassert>
#include <random>
#include <functional>
#include <stack>
using namespace std;

template<class T>
void in(vector<T>& V) {
	for (auto itr = V.begin(); itr != V.end(); itr++)cin >> *itr;
	return;
}

template<class T>
void out(vector<T>& V) {
	for (T& itr : V)cout << itr << endl;
	return;
}

vector<int> H;
vector<long long> DP;
int N,K;

int main() {
	cin >> N >> K;
	H.resize(N + 1, INT_MAX);
	for (int n = 1; n <= N; n++)cin >> H[n];
	DP.resize(N + 1);
	DP[0] = INT_MAX;
	DP[1] = 0;
	for (int n = 2; n <= N; n++) {
		DP[n] = INT_MAX;
		for (int nn = n - 1; nn >= max(n - K, 1); nn--) {
			DP[n] = min(DP[n], DP[nn] + abs(H[n] - H[nn]));
		}
	}
	cout << DP[N] << endl;
	return 0;
}
