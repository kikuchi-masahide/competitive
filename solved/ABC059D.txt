#include <iostream>
#include <algorithm>
#include <functional>
using namespace std;

int main(){
  long long x,y;
  cin >> x >> y;
  long long a,b;
  a = min(x,y),b = max(x,y);
  if(b-a <= 1)cout << "Brown" << endl;
  else cout << "Alice" << endl;
  return 0;
}
