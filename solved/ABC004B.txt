#include <iostream>
#include <vector>
#include <limits.h>
#include <algorithm>
#include <string>
#include <math.h>
#include <limits.h>
#include <queue>
#include <map>
#include <set>
#include <iomanip>
#include <bitset>
#include <cassert>
#include <random>
#include <functional>
#include <stack>
#include <iomanip>
#include <cassert>
using namespace std;
long long MOD = 1e9 + 7;

vector<vector<string>> V;

int main() {
	V.resize(4, vector<string>(4, string("")));
	for (int y = 0; y < 4; y++) {
		for (int x = 0; x < 4; x++)cin >> V[x][y];
	}
	for (int y = 0; y < 4; y++) {
		for (int x = 0; x < 4; x++) {
			cout << V[3 - x][3 - y] << " ";
		}
		cout << endl;
	}
	return 0;
}
