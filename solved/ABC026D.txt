#include <iostream>
#include <vector>
#include <limits.h>
#include <algorithm>
#include <string>
#include <math.h>
#include <limits.h>
#include <queue>
#include <map>
#include <set>
#include <iomanip>
#include <bitset>
#include <cassert>
#include <random>
#include <functional>
#include <stack>
#include <iomanip>
#include <cassert>
using namespace std;
long long MOD = 1e9 + 7;

long double A, B, C;

bool judge(long double t) {
	static long double PI = acos(-1);
	if (A * t + B * sinl(C * t * PI) - 100 >= 0)return true;
	else return false;
}

int main() {
	cin >> A >> B >> C;
	long double s = 0, e = 10000;
	int t = 0;
	for (; t < 1000; t++) {
		long double m = (e + s) / 2;
		if (judge(m))e = m;
		else s = m;
		//cout << setprecision(12) << m << endl;
		//cout << setprecision(12) << A * m + B * sinl(C * m * acosl(-1)) << endl;
	}
	cout << setprecision(12) << (e + s) / 2 << endl;
	return 0;
}
