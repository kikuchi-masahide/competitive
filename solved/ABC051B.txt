#include <iostream>

int K, S;
using namespace std;

int main() {
	cin >> K >> S;
	int sum = 0;
	for (int x = 0; x <= K; x++) {
		for (int y = 0; y <= K; y++) {
			if (0 <= S - x - y && S - x - y <= K)sum++;
		}
	}
	cout << sum << endl;
}