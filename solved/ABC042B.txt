#include <iostream>
#include <vector>
#include <limits.h>
#include <algorithm>
#include <string>
#include <math.h>
#include <limits.h>
#include <queue>
#include <map>
#include <set>
#include <iomanip>
#include <bitset>
#include <cassert>
#include <random>
#include <functional>
#include <stack>
#include <iomanip>
#include <cassert>
using namespace std;
long long MOD = 1e9 + 7;

int N, L;
vector<string> S;

int main() {
	cin >> N >> L;
	S.resize(N);
	for (int n = 0; n < N; n++) {
		cin >> S[n];
	}
	sort(S.begin(), S.end());
	for (int n = 0; n < N; n++) {
		cout << S[n];
	}
	cout << endl;
	return 0;
}
