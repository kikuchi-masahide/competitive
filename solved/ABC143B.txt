#include <iostream>
#include <vector>
#include <limits.h>
#include <algorithm>
#include <string>
#include <math.h>
#include <limits.h>
#include <queue>
#include <map>
#include <set>
#include <iomanip>
#include <bitset>
#include <cassert>
#include <random>
#include <functional>
#include <stack>
#include <iomanip>
#include <cassert>
using namespace std;
long long MOD = 1e9 + 7;

int N;
vector<int> D;

int main() {
	cin >> N;
	D.resize(N);
	for (int n = 0; n < N; n++)cin >> D[n];
	long long ans = 0;
	for (int a = 0; a < N; a++) {
		for (int b = a + 1; b < N; b++) {
			ans += D[a] * D[b];
		}
	}
	cout << ans << endl;
	return 0;
}
