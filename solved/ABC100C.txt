#include <iostream>
#include <vector>
#include <limits.h>
#include <algorithm>
#include <string>
#include <math.h>
#include <limits.h>
#include <queue>
#include <map>
#include <set>
#include <iomanip>
#include <bitset>
#include <cassert>
#include <random>
#include <functional>
#include <stack>
#include <iomanip>
#include <cassert>
using namespace std;
long long MOD = 1e9 + 7;

long long N;
vector<long long> A;

int main() {
	cin >> N;
	A.resize(N);
	for (long long n = 0; n < N; n++)cin >> A[n];
	long long ans = 0;
	for (long long n = 0; n < N; n++) {
		long long t = 0;
		while (A[n] % 2 == 0) {
			A[n] /= 2;
			t++;
		}
		ans += t;
	}
	cout << ans << endl;
	return 0;
}
