#include <iostream>
#include <vector>
#include <limits.h>
#include <algorithm>
#include <string>
#include <math.h>
#include <limits.h>
#include <queue>
#include <map>
#include <set>
#include <iomanip>
#include <bitset>
#include <cassert>
#include <random>
#include <functional>
#include <stack>
#include <iomanip>
#include <cassert>
using namespace std;
long long MOD = 1e9 + 7;

int N, X;
int sum, minm = INT_MAX;

int main() {
	cin >> N >> X;
	for (int n = 0; n < N; n++) {
		int m;
		cin >> m;
		sum += m;
		minm = min(minm, m);
	}
	cout << N + floor((long double)(X - sum) / minm) << endl;
	return 0;
}
