#include <iostream>
#include <vector>
#include <limits.h>
#include <algorithm>
#include <string>
#include <math.h>
#include <limits.h>
#include <queue>
#include <map>
#include <set>
#include <iomanip>
#include <bitset>
#include <cassert>
#include <random>
#include <functional>
#include <stack>
#include <iomanip>
using namespace std;

int N;

int main() {
	cin >> N;
	set<int> S;
	for (int n = 0; n < N; n++) {
		int a;
		cin >> a;
		while (!(a & 1))a >>= 1;
		S.insert(a);
	}
	cout << S.size() << endl;
	return 0;
}
