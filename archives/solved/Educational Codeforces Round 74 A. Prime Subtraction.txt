#include <iostream>
#include <vector>
#include <limits.h>
#include <algorithm>
#include <string>
#include <math.h>
#include <limits.h>
#include <queue>
#include <map>
#include <set>
#include <iomanip>
#include <bitset>
#include <cassert>
#include <random>
#include <functional>
#include <stack>
#include <iomanip>
using namespace std;
 
int main() {
	int Query;
	cin >> Query;
	for (int q = 0; q < Query; q++) {
		long long x, y;
		cin >> x >> y;
		if (x - y == 1)cout << "NO" << endl;
		else cout << "YES" << endl;
	}
	return 0;
}