#include <iostream>
#include <vector>
#include <limits.h>
#include <algorithm>
#include <string>
#include <math.h>
#include <limits.h>
#include <queue>
#include <map>
#include <set>
#include <iomanip>
#include <bitset>
#include <cassert>
#include <random>
#include <functional>
#include <stack>
#include <iomanip>
#include <cassert>
using namespace std;
long long MOD = 1e9 + 7;
vector<int> V;

int main() {
	int N;
	cin >> N;
	V.resize(6);
	N -= (N / 30) * 30;
	for (int n = 0; n < 6; n++)V[n] = n + 1;
	for (int n = 0; n < N; n++) {
		swap(V[(n ) % 5], V[(n ) % 5 + 1]);

	}
	for (int n = 0; n < 6; n++)cout << V[n];
	cout << endl;
	return 0;
}
