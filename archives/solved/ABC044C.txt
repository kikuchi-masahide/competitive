#include <iostream>

#include <vector>
#include <limits.h>
#include <algorithm>
#include <string>
#include <math.h>
#include <limits.h>
#include <queue>
#include <map>
#include <set>
#include <iomanip>
#include <bitset>
#include <cassert>
#include <random>
#include <functional>
#include <stack>
using namespace std;

template<class T>
void in(vector<T>& V) {
	for (auto itr = V.begin(); itr != V.end(); itr++)cin >> *itr;
	return;
}

template<class T>
void out(vector<T>& V) {
	for (T& itr : V)cout << itr << endl;
	return;
}

vector<int> xa;
long long suma[51][51][2501];   //[–‡”][˜a]
int N, A;

int main() {
	cin >> N >> A;
	xa.resize(N);
	in(xa);
	//for (int codeb = 0; codeb < (1 << (N - N / 2)); codeb++) {
	//	int nn = 0, SS = 0;
	//	for (int n = 0; n < N - N / 2; n++) {
	//		if ((codeb >> n) & 1) {
	//			nn++;
	//			SS += xb[n];
	//		}
	//	}
	//	sumb[nn][SS]++;
	//}
	//for (int codea = 0; codea < (1 << (N / 2)); codea++) {
	//	int n = 0, S = 0;
	//	for (int c = 0; c < N / 2; c++) {
	//		if ((codea >> c) & 1) {
	//			n++;
	//			S += xa[c];
	//		}
	//	}
	//	suma[n][S]++;
	//}
	suma[0][0][0] = 1;
	for (int n = 1; n <= N; n++) {
		for (int used = 0; used <= n; used++) {
			for (int s = 0; s <= 2500; s++)suma[n][used][s] = suma[n - 1][used][s];
		}
		for (int used = 1; used <= n; used++) {
			for (int s = xa[n-1]; s <= 2500; s++) {
				suma[n][used][s] += suma[n-1][used - 1][s - xa[n-1]];
			}
		}
	}
	long long ans = 0;
	for (int n = 1; n <= N; n++) {
		ans += suma[N][n][A * n];
	}
	cout << ans << endl;
	return 0;
}
