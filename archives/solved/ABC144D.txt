#include <iostream>
#include <vector>
#include <limits.h>
#include <algorithm>
#include <string>
#include <math.h>
#include <limits.h>
#include <queue>
#include <map>
#include <set>
#include <iomanip>
#include <bitset>
#include <cassert>
#include <random>
#include <functional>
#include <stack>
#include <iomanip>
#include <cassert>

using namespace std;
long long MOD = 1e9 + 7;

const long double PI = 3.1415926535;

int main() {
	long double a, b, x;
	cin >> a >> b >> x;
	long double t = 2 * (b*a*a - x ) / (a*a*a);
	if (a * a * b / 2 <= x)cout << setprecision(12) << atan2l(2 * (b * a * a - x), a * a * a) / acos(-1) * 180 << endl;
	else cout << setprecision(12) << atan2l(a * b * b, 2 * x) / acos(-1) * 180 << endl;
	return 0;
}
