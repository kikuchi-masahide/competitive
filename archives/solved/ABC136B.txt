#include <iostream>
#include <vector>
#include <limits.h>
#include <algorithm>
#include <string>
#include <math.h>
#include <limits.h>
#include <queue>
#include <map>
#include <set>
#include <iomanip>
#include <bitset>
#include <cassert>
#include <random>
#include <functional>
#include <stack>
#include <iomanip>
#include <cassert>
using namespace std;
long long MOD = 1e9 + 7;

int N;

int main() {
	cin >> N;
	int ans = 0;
	for (int n = 1; n <= N; n++) {
		int counter = 0;
		int copy = n;
		while (copy > 0) {
			copy /= 10;
			counter++;
		}
		if (counter%2==1)ans++;
	}
	cout << ans << endl;
	return 0;
}
