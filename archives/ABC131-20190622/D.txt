void solve();

int main() {
	solve();
	return 0;
}

//////////////////////////////////////////////////
//////////////////////////////////////////////////
#include <iostream>
#include <vector>
#include <algorithm>
#include <limits.h>
#include <map>
#include <queue>
#include <iomanip>
#include <set>
using namespace std;
typedef pair<int, int> pii;

template<class T>
T GCD(T a, T b) {
	if (b == 0)return a;
	return GCD(b, (T)(a % b));
}

template<class T>
T LCM(T a, T b) {
	return a * b / GCD(a, b);
}

long long A, B, C, D;

long long num(long long N) {
	return N - (N/C+N/D-N/LCM(C,D));
}

void solve() {
	cin >> A >> B >> C >> D;
	cout << num(B) - num(A - 1) << endl;
	return;
}
