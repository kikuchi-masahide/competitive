void solve();

int main() {
	solve();
	return 0;
}

//////////////////////////////////////////////////
//////////////////////////////////////////////////
#include <iostream>
#include <vector>
#include <algorithm>
#include <limits.h>
#include <map>
#include <queue>
#include <iomanip>
#include <set>
using namespace std;
typedef pair<int, int> pii;

void solve() {
	int N, L;
	cin >> N >> L;
	int sum = (L + N - 1) * (L + N) / 2 - L*(L-1)/2;
	if (L > 0)cout << sum - L << endl;
	else if (L + N - 1 >= 0)cout << sum << endl;
	else cout << sum - (L + N - 1) << endl;
	return;
}
