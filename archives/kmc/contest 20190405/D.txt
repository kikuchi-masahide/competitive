#include <iostream>
#include <algorithm>
using namespace std;

int main(){
    while(true){
        int a,b;
        cin >> a >> b;
        if(a == 0 && b == 0)return 0;
        cout << min(a,b) << " " << max(a,b) << endl;
    }
}