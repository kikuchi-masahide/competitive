void solve();

int main() {
	solve();
	return 0;
}

//////////////////////////////////////////////////
//////////////////////////////////////////////////
#include <iostream>
#include <vector>
#include <algorithm>
#include <limits.h>
#include <map>
#include <queue>
#include <iomanip>
#include <set>
using namespace std;
typedef pair<int, int> pii;

int N, K;

void solve() {
	cin >> N >> K;
	if (K > (N - 1) * (N - 2) / 2) {
		cout << -1 << endl;
		return;
	}
	K = (N - 1) * (N - 2) / 2 - K;
	cout << (N - 1) + K << endl;
	for (int i = 2; i <= N; i++)cout << "1 " << i << endl;
	for (int i = 2; i <= N && K > 0; i++) {
		for (int j = i+1; j <= N && K > 0; j++) {
			cout << i << " " << j << endl;
			K--;
		}
	}
	int a = 1;
	return;
}
